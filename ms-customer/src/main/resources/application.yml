spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userms-mysql
    username: dev
    password: fiap
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  file:
    path: ./var/log
