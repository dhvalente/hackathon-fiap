server:
  port: 8080
spring:
  application:
    name: micro-reservas
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  rabbitmq-channel:
    exchangeName: evento.topic
    queueName: notificacao
    routingKey: notificacao.email